ollama/data
ollama/models

open-webui/data

paperless/consume
paperless/data
paperless/export
paperless/media

postgres/data

redis/data

# Project-root .env holds secrets (PAPERLESS_API_TOKEN). Anchored to repo
# root so per-service ./<svc>/.env files (paperless/, postgres/, translator/, …)
# remain tracked.
/.env
